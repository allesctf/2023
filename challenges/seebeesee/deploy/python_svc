service seebeesee
{
type         = UNLISTED
protocol     = tcp
socket_type  = stream
port         = 1024
server       = /usr/bin/env
server_args  = pypy3 -u /opt/seebeesee.py
wait         = no
user         = root
env          = HOME=/opt
}
